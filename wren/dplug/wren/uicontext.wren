// API for global UI things. Wraps an UIContext.
class UI {


   // Get current width of the UI, in user pixels.
   foreign static width

   // Get current height of the UI, in user pixels.
   foreign static height


}
